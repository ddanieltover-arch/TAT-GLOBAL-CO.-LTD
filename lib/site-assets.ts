/** Public paths for downloadable assets (files live under `public/`). */
export const CATALOG_PDF = {
  path: '/catalog/TAT-Global-Catalog.pdf',
  filename: 'TAT-Global-Catalog.pdf',
} as const;

/** Brand logo (source: company master artwork). */
export const brandLogo = {
  full: '/images/logo.webp',
  header: '/images/logo-header.webp',
  png: '/images/logo.png',
} as const;
